#include <stdio.h>
 
int main() {
    int A, B, C, D;
    int DIFERENCA = 0;
    
    scanf("%d\n%d\n%d\n%d", &A, &B, &C, &D);
    DIFERENCA = (A * B) - (C * D);
    
    printf("DIFERENCA = %d\n", DIFERENCA);
 
    return 0;
}
